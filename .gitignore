# OS cruft
.DS_Store
Thumbs.db

# nothing here needs node, but keep it safe if anyone runs npm in the repo
node_modules/

# never
.env
.env.*
!.env.example

# Python bytecode. A .pyc built by one interpreter version is dead weight
# for everyone else, and this package is meant to be cloned and read.
__pycache__/
*.pyc
